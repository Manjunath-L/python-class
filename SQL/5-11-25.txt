WAP to display FNAME,LNAME,ANALsal form emp:
	select fname,lname,sal*12
 	from emp;

WAP to FNAME,LNAME AND SAL with 10% HIKE:
	col + (sal*%hike/100)
	select fname,lname,sal+(sal*10/100)
	from emp;

WAP to Fname,lname,job and sal with 15% dec:
	col - (sal*%hike/100)
	select fname,lname,sal-(sal*15/100),job
	from emp;

wqtd DETAILS OF EMP ALONG WITH ANNUAL SAL WITH 10% HIKE
	SELECT *,SAL*12+(SAL*12)*10/100
	FROM EMP;

wqtd DETAILS OF EMP AND SAL WITH 12% DEDUCTION AND COMM WITH 5% HIKE:
	SELECT *,sal-(sal*12/100),COMM+(COMM*5/100)
	FROM EMP;

ALIASING NAMEING:
mysql> SELECT *,sal-(sal*12/100) 'SALARY WITH 12% DECDU',COMM+(COMM*5/100) 'COMM WITH 5% HIKE'
    ->  FROM EMP;

WQTD FNAME AS FIRST NAME , LNAME AS LAST NAME AND ANNUAL SALARY FROM EMP:
     SELECT FNAME 'FIRST NAME', LNAME 'LAST NAME', SAL*12 'ANNUAL SALARY'
     FROM EMP;

WQTD DIFFRENT JOB FORM EMP:

	SELECT DISTINCT JOB , SAL
	FROM EMP;

WQTD DEATILS OF EMP WHO FNAME IS KARAN?
SELECT * 
FROM EMP
WHERE FNAME = "KARAN";

WQTD FNAME LNAME AND JOB FROM TABLE IF THE EMP WORKING AS SALESMAN
SELECT FNAME,LNAME,JOB
FROM EMP
WHERE JOB = 'SALESMAN';

WQTD DETAILS OF EMP WHO WERE BROM AFTER THE YER 1993?
SELECT * 
FROM EMP
WHERE DOB > 1993-01-01;

OR 
WHERE DOB >= '1994-01-01';

WQTD DETAILS OF EMP WHO WERE HIRED BEFORE 2019;

SELECT *
FROM EMP
WHERE DOJ < '2019-01-01';