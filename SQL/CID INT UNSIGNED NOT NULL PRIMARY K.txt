CID INT UNSIGNED NOT NULL PRIMARY KEY AUTOINCREMENT
CNAME VARCHAR(20) NOTNULL
PHONE BINGINT UNSIGNED NN UNIQUE CHCK(LENGTH(PHONE)=10)
BALANCE DECIMAL(5,2) DEAFULT'100'
ID INT UNSIGNED
CONSTRAINT FOREIGN KEY(StudentID)REFERENCES STUDENT_DATA(StudentID);

CREATE TABLE CUSTOMER (
    CID INT UNSIGNED NOT NULL PRIMARY KEY AUTO_INCREMENT,
    CNAME VARCHAR(20) NOT NULL,
    GENDER ENUM('MALE','FEMALE') NOT NULL,
    PHONE BIGINT UNSIGNED NOT NULL UNIQUE CHECK (LENGTH(PHONE) = 10),
    BALANCE DECIMAL(5,2) DEFAULT 100.00,
    ID INT ,
    CONSTRAINT FOREIGN KEY(ID) REFERENCES student(StudentID)
);

##################################################

PID INT UNSIGNED NN PRIMARY KEY
PNAME VARCHAR(20), NN
QUANTITY INT UNSIGNED DEFAULT'2'
PRICE DECIMAL(7,2) UNSIGNED NN

CREATE TABLE PRODUCT(
PID INT UNSIGNED NOT NULL PRIMARY KEY,
PNAME VARCHAR(20) NOT NULL,
QUANTITY INT UNSIGNED DEFAULT 2,
PRICE DECIMAL(7,2) UNSIGNED NOT NULL
);

CREATE TABLE PRODUCT(
    PID INT UNSIGNED NOT NULL PRIMARY KEY,
    PNAME VARCHAR(20) NOT NULL,
    QUANTITY INT UNSIGNED DEFAULT 2,
    PRICE DECIMAL(7,2) UNSIGNED NOT NULL
);

1. TO ADD COLUMN
`````````````````

create table faculty(
FID INT UNSIGNED NOT NULL
);
